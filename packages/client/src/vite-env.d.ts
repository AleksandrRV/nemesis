/// <reference types="vite/client" />

/**
 * Версия приложения: подставляется сборкой из `packages/client/package.json`
 * (см. `define` в vite.config.ts) и показывается в HUD.
 */
declare const __APP_VERSION__: string;
