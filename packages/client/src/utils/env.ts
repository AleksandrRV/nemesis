/**
 * Признак dev-сборки Vite.
 *
 * От него зависит доступность отладочных действий (переключатели дверей и шума):
 * в продакшн-сборке `import.meta.env.DEV` равен false, поэтому движок отклонит
 * такие действия, даже если вызвать их напрямую из консоли браузера.
 */
export const IS_DEV = import.meta.env.DEV === true;
