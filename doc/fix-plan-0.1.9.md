# План исправлений в три этапа — от 0.1.9 к «0.2»

**Дата:** разбор ревизии `0.1.9` (см. `doc/review-0.1.9.md`)
**Основание:** критерии ревизии `0.1.0`. Сам документ `doc/audit-0.1.0.md`, на который ссылаются
README и комментарии в коде, **в репозитории отсутствует**, поэтому таблица критериев в §1
собрана по фактическим цитатам: пункты аудита `№6`, `№7`, `№10`, `№22`, `№25`, `§3.1`, `§3.3`,
`§4 P1`, `§4 P1-1`, `§4 P1-4`, плюс `AGENTS.md §1–§4, §7`, результат этапа 1 дорожной карты и
утверждения README. Если пришлёте полный список пунктов `№1–№25` — досопоставлю и уточню порядок.

---

## 1. Критерии 0.1.0 и их состояние в 0.1.9

| Критерий (источник)                                                        | Требование                                                                     | Состояние в 0.1.9                                              |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------------------- |
| Клиент только отправляет действия (аудит `№6`)                              | Клиент не мутирует `GameState`, любое изменение — `dispatch(action)`           | ✅ соблюдён                                                     |
| Скрытое не покидает движок (аудит `№7`, `№10`, `AGENTS §3.4`)               | Наружу — только `SanitizedGameState`; «нулевое читерство» как свойство данных   | ⚠️ **нарушен частично**: порядок мешка, порядок колод и состав чужой руки видны в срезе |
| Запрет читерских элементов UI, dev-действия только в dev (аудит `№22`, `§4 P1-4`) | Отладочный инструментарий не попадает в продакшн                               | ✅ соблюдён (тест + проверка бандла)                            |
| Сид виден и партия воспроизводима (аудит `§4 P1-1`)                         | Один сид — один стол; сид показан игроку и пригоден для отчёта                  | ⚠️ **нарушен на LAN/HTTP**: `crypto.randomUUID` недоступен → белый экран |
| Нереализованное отклоняется явной ошибкой (аудит `§4 P1`, `AGENTS §1.3`)     | Никаких «полумеханик» и молчаливого «ничего не произошло»                      | ❌ **нарушен**: вскрытие отсека разыгрывается наполовину (нет Шума и эффекта жетона) |
| Ядро изоморфно: без DOM, React, Node, сети (`AGENTS §2.1`)                  | `packages/shared` запускается и в браузере, и в Node                            | ✅ соблюдён, границы enforced линтером                          |
| Типизированные контракты, без `any` (аудит `№25`, `AGENTS §4.1`)            | Discriminated unions, подписи из контракта, `strict`                            | ✅ соблюдён                                                     |
| Правила и данные 1:1 с оригиналом (`AGENTS §1.1`, `§7`)                     | Тексты, числа, состав колод и поля — строго по книге и физическим компонентам   | ❌ **не подтверждён**: источников сверки в репозитории нет       |
| Колоды — это данные (аудит `§3.1`, `§3.3`)                                  | Состав карт описан данными, а не структурой                                     | ⚠️ осознанно не сделано (этап 3 дорожной карты)                 |
| Этап 1 дорожной карты: карта, зум, инспектор отсека                          | Отсеки, коридоры, двери, панорамирование, панель отсека                          | ✅ соблюдён                                                     |
| Мобильная сессия сохраняется (`AGENTS §4.4`)                                | Партия переживает перезагрузку вкладки                                          | ⚠️ частично: сохранение есть, но нет офлайн-старта (PWA) и есть падение на HTTP |

**Вывод:** из проверяемых критериев 0.1.0 три нарушены (читерство в срезе, сид на HTTP,
полумеханика вскрытия), один не подтверждён (данные 1:1). Этап 1 плана закрывает первые три,
этап 2 — четвёртый, этап 3 — готовит следующие этапы дорожной карты.

---

## 2. Этап 1 — «Критерии 0.1.0 снова выполняются» (2–4 дня)

Цель: вернуть честность текущего среза малыми правками, не добавляя новых механик.
После этапа 1 любой пункт таблицы §1 либо соблюдён, либо отклоняется явной ошибкой.

| ID    | Исправление                                                                                                                                                                | Почему сейчас                    | Критерий приёмки                                                                                    |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------- |
| Э1-1  | **Тасовать мешок Чужих** потоком `bag` при подготовке (`setup.ts:336`) и **убрать порядок из среза**: в `SanitizedGameState` мешок — счётчики по типам, не массив жетонов     | Контакты всегда шли бы одним и тем же сценарием; порядок виден игроку и ботам | Тест: два сида дают разный порядок; в срезе нет порядка жетонов; версия схемы поднята               |
| Э1-2  | **Сид без secure context**: UUIDv4 через `crypto.getRandomValues`, а `randomUUID` — быстрый путь, если доступен (`seed.ts:10`)                                               | Первый запуск с телефона по `http://<LAN-IP>` падает целиком | Тест с `crypto` без `randomUUID`: партия стартует; вручную проверено на LAN-IP                       |
| Э1-3  | **Прекратить полумеханику вскрытия отсека.** Вариант A (0,5 дня): вход в неисследованный отсек отклоняется ошибкой `EXPLORE_ROOM_NOT_IMPLEMENTED` до Э2-3. Вариант B (1–1,5 дня): сохранить жетон в отсеке, разыграть эффект (Огонь/Поломка/Слизь/Двери), бросить d10 Шума (1–4 — маркер, Тишина/Опасность — без броска), а Контакт отклонять явной ошибкой до этапа 4 | Сейчас игрок получает неверную партию, и ошибки нет | Нет ни одного пути, где правило разыгрывается частично и молча; на каждый путь — тест с ожидаемой ошибкой или результатом |
| Э1-4  | **«Замки» на критерии в тестах**: срез не содержит порядка мешка/колод/чужой руки; dev-действия запрещены без флага; `DEV-ПАНЕЛЬ` отсутствует в прод-сборке; партия восстанавливается из сохранения | Критерии держатся дисциплиной, а не проверками | Тесты падают при регрессии любого из четырёх критериев                                              |
| Э1-5  | **CI-минимум**: GitHub Actions — `npm run verify` и `npm run build` на push/PR; бандл как артефакт                                                                          | Регрессии сейчас ловит только разработчик | Красный CI на сломанном `verify`; успешная сборка на каждый PR                                      |
| Э1-6  | **Указатели и версия**: вернуть или заменить ссылки на `doc/audit-0.1.0.md` (README + комментарии); пометить `tech_stack.md`/GDD как «цель» против «сейчас»; снять из README переобещания («сеть не требуется», «1–5 игроков»); CHANGELOG и тег `v0.1.9` | 10+ висячих ссылок, документация описывает несуществующее | Ни одной ссылки в никуда; CHANGELOG и тег есть; README совпадает с фактическим поведением             |

**Готовность этапа:** таблица §1 — без строк «нарушен» и «не подтверждён» (для данных 1:1 на
этом этапе допускается статус «проверяется в этапе 2» с явной пометкой в README).

---

## 3. Этап 2 — «Данные и правила 1:1» (1–2 недели)

Цель: сделать утверждение «перенос 1:1» проверяемым и завершить дорожный этап 2
(движение, туман войны, шум) без новых крупных подсистем.

| ID    | Исправление                                                                                                                                                                         | Критерий приёмки                                                                                   |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Э2-1  | **Пакет источника + происхождение данных.** Сверить с физическими компонентами: пул жетонов Исследования (в коробке 20, а в коде 16 с суммой 34), числа Внезапной атаки на жетонах Чужих, топологию коридоров и номера входа в вентиляцию, символы компьютеров, номера и координаты Машинных отсеков. Рядом с каждой таблицей — происхождение (страница правил, фото, кто сверял) | У каждой таблицы есть источник в репозитории; golden-тесты сверяют данные с источником, а не сами с собой |
| Э2-2  | **Двери и запасы маркеров.** «Разрушенная» — терминальное состояние (нельзя снова закрыть, rules.md:1044); запас 12 жетонов Дверей с правилом перестановки (rules.md:1047); один жетон на коридор; лимиты 8 пожаров / 8 поломок / 30 шума                 | Тест на каждое правило; dev-переключатель больше не может «починить» разрушенную Дверь               |
| Э2-3  | **Завершение движения, вскрытия и Шума** (дорожный этап 2): жетон Исследования хранится в отсеке; эффекты Тишина/Опасность/Слизь/Пожар/Поломка/Двери; d10 Шума с учётом Слизи и занятости отсека; маркеры в коридорах и в техкоридорах; «Осторожное движение» `[1]`; Контакт — явная ошибка до этапа 4 | Каждая механика имеет ссылку на страницу правил и тест; ни одного «молчаливого» пропуска            |
| Э2-4  | **RNG-потоки в состоянии.** Хранить счётчики обращений по потокам (или честно убрать обещание из `rng.ts:59–70`); подключить потоки `noise` и `bag`                                                                     | Сохранение и восстановление не сдвигает случайность; тест воспроизводимости после перезагрузки      |
| Э2-5  | **Санитайзер колод.** Скрывать состав и порядок чужих колод, руки и сброса; наружу — только количество карт (оно влияет на Внезапную атаку)                                            | Тест: срез не содержит идентичностей карт в чужих колодах; бот на `SanitizedGameState` не может читерить |
| Э2-6  | **Версия схемы и совместимость.** Поднять `GAME_STATE_SCHEMA_VERSION`; явно описать поведение при несовместимом сохранении; тест на «старое/битое сохранение — новая партия»           | Смена контракта не ломает молча; тест на отказ и на восстановление                                  |

**Готовность этапа:** в README 1:1-claim подтверждён ссылками на источник; дорожный этап 2
закрыт; ни одна реализованная механика не расходится с книгой правил.

---

## 4. Этап 3 — «Фундамент для этапа 3+ и качество» (2–4 недели)

Цель: подготовить рост проекта (колоды, действия, интерфейс партии) и снять инфраструктурные риски.

| ID    | Исправление                                                                                                                                          | Критерий приёмки                                                                     |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Э3-1  | **Колоды как данные + Фаза игроков** (дорожный этап 3): три колоды Предметов, Тяжёлые Травы, События, Цели, Слабости, Заражение; рука/добор/сброс; оплата действий; микрораунды «2 действия / пас»; действия комнат `[2]` | Партия играется по этапу 3 дорожной карты; запрет оплаты Заражением покрыт тестом     |
| Э3-2  | **UI партии и мобильность**: нижняя шторка с рукой карт, модальные окна действий, инспектор; доступность (роли и фокус на карте, отказ от `user-scalable=no`, `aria-label` у кнопок зума); локальные шрифты; `ErrorBoundary` | Карта управляется с клавиатуры; приложение стартует без сети; ни одного белого экрана на исключении |
| Э3-3  | **Инфраструктура качества**: пороги покрытия (ядро: statements ≥ 95%, branches ≥ 85%), DOM-тесты (jsdom + testing-library), E2E-дым «старт → перемещение → перезагрузка → продолжение», обновление Vite/vitest (5 уязвимостей), `engines`/`packageManager`, dependabot | Порог роняет CI при снижении покрытия; E2E зелёный; `npm audit` без high                   |
| Э3-4  | **Офлайн и подготовка сервера** (этапы 9–10 дорожной карты): service worker и манифест, звук; вынос генерации колод и мешка за пределы клиента          | Приложение ставится на телефон и работает в авиарежиме; порядок скрытых колод не покидает сервер |
| Э3-5  | **Релизный процесс**: теги на версии, CHANGELOG, метрики «доля механик, сверенных с оригиналом», бюджет бандла (сейчас 77 кБ gzip)                       | По тегу воспроизводится сборка; CHANGELOG ведётся; бюджет бандла не превышен                |

---

## 5. Порядок, зависимости и границы

- **Порядок жёсткий:** этап 1 → этап 2 → этап 3. Этап 1 трогает контракт состояния
  (Э1-1, Э1-3), поэтому делать его после Э2-3 — значит дважды менять `GAME_STATE_SCHEMA_VERSION`
  и дважды ломать сохранения.
- **Внутри этапа 2** Э2-1 (данные) выполняется первым: от свёренных чисел зависят Э2-2 и Э2-3.
- **Что не входит** в план: бой, Чужие на поле, боты, сервер, режим Чужого, альтернативное поле —
  это отдельные этапы дорожной карты (4, 8, 10, 11); здесь только то, что мешает считать
  текущий срез честным и воспроизводимым.
- **Метрики этапа 1:** 0 висячих ссылок, 0 «полумеханик» без явной ошибки, 100% критериев §1
  в статусе «соблюдён», CI зелёный на `verify` + `build`.
