# История изменений

Формат — по версиям пакета (`package.json`): патч закрывает дефекты и честность среза,
минор — новые механики. Версия показывается в HUD и подставляется сборкой из `package.json`.

## 0.4.0 — 2026-09-21

Этап 0.4.0 «Пул Чужих, Контакты и Тактический Бой» завершён (Шаги 1–8). Шаг 8: классовые боевые карты, аудит защиты информации, сквозная интеграция.

### Добавлено

- **Классовые боевые карты (`fsm.ts`, `combat.ts`)**: все 5 эффектов через `ACTION_PLAY_CARD` — «Стрельба очередью» (весь боезапас Винтовки, +1 Рана за каждые 2 ед. поверх кубика), «Прицельный огонь» (выстрел + решение `CHOOSE_AIMED_REROLL` с видимой первой гранью), «Заградительный огонь» / «Огонь на подавление» (1 боезапас, увод себя и/или другого без Атак Чужих), «Адреналин» (выстрел или настоящий Побег + добор карты). Принятые решения: первая грань переброса видна только стрелку; «другой» и цель — в том же отсеке; боезапас увода — из первого заряженного оружия в руках.
- **Выстрел по частям (`combat.ts`)**: `performShoot` разложен на бросок (`drawCombatFace`) и применение (`applyShootFace`) — переброс и бонусы без дублирования; движение/Побег переиспользуются через `startMoveOrEscape`.
- **UI**: наведение боевых карт в подтверждении розыгрыша (цель, оружие, отсек, кого увести, режим Адреналина) с блокировкой неполного выбора; модалка переброса «Оставить / Перебросить».
- **Тесты**: 4 (очередь) + 13 (карты и решения) + сквозной цикл «шум → Контакт → очередь с убийством → Останки → Побег»; цензура колоды Атак и приватность переброса.

### Защита информации

- Аудит пройден: порядок колоды Атак и мешка скрыт (числа и состав), сбросы Атак открыты, журнал хранит только свершившиеся факты (грани, попадания, раны), чужие решения невидимы.

### Исправлено

- Формулировки плана: цензура мешка (состав открыт по книге) и файлы интеграционных тестов (по принадлежности механики).

## 0.2.18 — 2026-09-21

Шаг 7 этапа 0.4.0: Побег из боя — выход из отсека с Чужими разыгрывает внеочередную атаку каждой особи:

### Добавлено

- **Прерывание (`fsm.ts`, `contact.ts`)**: `ACTION_MOVE` из отсека с Чужими кладёт в очередь `ESCAPE_ATTACK_INTERRUPT` (тип из объявленного контракта); обработчик разыгрывает атаки снимка и, если персонаж выжил, завершает шаг с обычным броском Шума.
- **Общая процедура (`contact.ts`)**: `resolveEscapeAttacks` + выделенное ядро `resolveIntruderAttackOnPlayer` — та же Атака Чужих, что при Внезапной (стр. 20); событие `ESCAPE_ATTACK_RESOLVED` на каждую особь. Личинка заражает без карты, «Зов» и «Трансформация» не дают лишних атак.
- **Гибель в Побеге**: перемещения и Шума нет, Труп остаётся в отсеке, ход закрывает `settleDeadActivePlayer`.
- **UI**: подтверждение «В отсеке находятся Чужие! … Бежать?» с кнопками «Бежать [цена: 1]» / «Отмена», статическое предупреждение «Побег: Чужие в отсеке атакуют в спину!»; урон отступления виден в журнале (`ESCAPE_ATTACK_RESOLVED`: мимо/раны/заражение/гибель).
- **Тесты**: промах и попадание, очередь атак, гибель без перемещения и Шума, Личинка без карты, «Зов», «Трансформация», цепочка прерывания, форматы журнала и предупреждение инспектора.

### Исправлено

- Имя прерывания в плане (`INTRUDER_ATTACK_INTERRUPT`) приведено к объявленному контракту: `ESCAPE_ATTACK_INTERRUPT`.

## 0.2.17 — 2026-09-21

Шаг 6 этапа 0.4.0: гибель Чужих оставляет Останки, Тяжёлые Объекты подбираются, Лаборатория раскрывает Слабости:

### Добавлено

- **Останки (`combat.ts`)**: гибель Чужого выкладывает на пол `INTRUDER_REMAINS` с типом погибшего — от любого, кроме Личинки (стр. 22); id уникальны при повторной гибели того же жетона.
- **Подбор (`packages/shared/src/logic/objects.ts`)**: действие `ACTION_PICK_UP_OBJECT` [1] — Труп, Останки или Яйцо с пола своего отсека в свободный слот руки; событие `OBJECT_PICKED_UP`.
- **Слабости (`packages/shared/src/data/weaknessCards.ts`, `setup.ts`)**: 7 карт из doc/data/INTRUDERS.md §6, сетап раздаёт 3 случайные рубашкой вверх потоком `layout`.
- **Лаборатория (`roomAbilities.ts`)**: изученный объект остаётся на полу (стр. 22), в журнал пишется имя раскрытой Слабости; пустой слот отклоняется явной ошибкой вместо молчания.
- **UI**: кнопка «Поднять [цена: 1]» у объектов на полу (когда игрок в отсеке); общие подписи `HEAVY_OBJECT_LABELS`.
- **Тесты**: Останки (типы, Личинка, Королева без яйца, уникальность id), подбор (условия, цикл гибель→подбор, оплата), раздача слабостей, Лаборатория, форматы журнала и инспектор.

### Исправлено

- Пункт плана про яйцо за Королеву приведён к книге: такого правила нет (все 5 жетонов Яиц — для Улья), Королева оставляет только Останки.
- Указание шага в ошибке Огнемёта: механика огня — этап 0.5.0, а не Шаг 6.

### Известные ограничения

- Восьмая карта Слабости отсутствует в транскрипте (книга говорит о 8) — будет добавлена с её текстом.
- Эффекты раскрытых Слабостей движок пока не применяет (будущий этап за пределами 0.4.0).

## 0.2.16 — 2026-09-21

Шаг 5 этапа 0.4.0: базовое действие «Рукопашная Атака» [1] — отчаянная атака без патронов с гарантированной ценой:

### Добавлено

- **Рукопашная (`packages/shared/src/logic/combat.ts`, `fsm.ts`)**:
  - Действие `ACTION_MELEE`: цель в том же отсеке, оплата 1 картой Действия; оружие и патроны не требуются.
  - Порядок по книге (стр. 19): сначала 1 Заражение в сброс, затем бросок кубика Боя через поток `combat`.
  - Маппинг граней: [++] наносит лишь 1 Рану; Хвост и Силуэты по невосприимчивому типу — промах.
  - Промах наносит атакующему 1 Тяжёлую Травму (может убить — следом идёт `PLAYER_DIED`); попадание — Раны и общая со стрельбой проверка Стойкости.
  - Событие журнала `MELEE_ATTACKED`: грань, Раны, Заражение и Травма.
- **Модалка «Рукопашная атака» (`MeleeModal.tsx`)**: бейджи «+1 Заражение» и «Риск Тяжёлой Травмы при промахе», выбор цели, результат из журнала; кнопка «Рукопашная атака [цена: 1]» с бейджами в действиях инспектора.
- **Общий `CombatResultView.tsx`**: результат боя для обеих модалок (бросок, цена, Стойкость, гибель/отступление Чужого, гибель атакующего).
- **Тесты**: 31 тест рукопашной (матрица граней, иммунитет-как-промах, порядок Заражения, исчерпание колод, смертельная Травма, оплата), форматы журнала и модалка.

## 0.2.15 — 2026-09-21

Шаг 4 этапа 0.4.0: базовое действие «Стрельба» [1] — выстрел, кубик Боя, проверка Стойкости, гибель и отступление Чужих:

### Добавлено

- **Стрельба (`packages/shared/src/logic/combat.ts`, `fsm.ts`)**:
  - Действие `ACTION_SHOOT`: цель в том же отсеке, оружие в руке с ≥1 Боезапаса, оплата 1 картой Действия; сброс 1 Боезапаса и бросок кубика Боя через поток `combat`.
  - Маппинг граней по типу цели (стр. 18): Хвост — только Личинка/Крипер, Силуэты — все, кроме Трутня/Королевы, [1]/[2] Раны — любому.
  - Особые свойства оружия (стр. 22): Пистолет и Револьвер ([2 Раны] → 1), Обрез (Силуэты — промах), Дробовик и Боевая винтовка (+1 Рана), Огнемёт (минимум 1 Рана, кроме Промаха).
  - Проверка Стойкости: Личинка гибнет от любой Раны без карты; Крипер/Взрослая — 1 карта; Трутень/Королева — 2 карты с суммой; стрелка Отступления перекрывает смерть.
  - События журнала: `SHOT_FIRED`, `TOUGHNESS_CHECKED`, `INTRUDER_KILLED`, `INTRUDER_RETREATED`.
- **Модалка «Стрельба» (`ShootModal.tsx`)**: выбор оружия и цели, показ броска, карт Стойкости и исхода; кнопка «Стрельба [цена: 1]» в действиях инспектора при нахождении в Бою.
- **Тесты**: 53 теста боя (матрица граней × типы × оружие, Стойкость, отступление, оплата и счётчик действий), форматы журнала и модалка.

### Промежуточные правила (заменятся позже)

- **Отступление без колоды Событий**: по книге направление задаёт карта События (стр. 18), но колода появится в 0.5.0 — до тех пор направление выбирает поток `combat` среди соседних отсеков через открытые Двери; вентиляция исключена, без открытых дверей Чужой остаётся на месте и выживает.
- **Огнемёт при [2 Ранах]** требует маркера Пожара (этап 0.5.0: урон от огня резолвится в Фазе Событий): такой выстрел отклоняется ошибкой `SHOOT_FIRE_NOT_IMPLEMENTED`.
- **Останки** убитых Чужих не выкладываются (Шаг 6): жетоны уходят в `deadTokens`.

### Исправлено

- Сетап выдавал оружие ссылкой на общий объект данных: выстрелы одной партии тратили боезапас всех следующих. Теперь каждая партия получает независимую копию.

## 0.2.14 — 2026-09-21

Шаг 3 этапа 0.4.0: Чужие видны на карте и в инспекторе отсека. Поведение движка не менялось — привязка монстров к отсекам, статус Боя и передача Чужих в срез уже были реализованы ранее:

### Добавлено

- **Значки Чужих на карте (`RoomHex.tsx`, `ShipMapSVG.tsx`)**:
  - Под гексом отсека — значок каждой особи цветом типа: зелёная Личинка, жёлтый Крипер, красная Взрослая Особь, бордовый Трутень, фиолетовая Королева.
  - Счётчик полученных ран на значке и подсказка с типом и числом ран.
- **Блок «Чужие в отсеке» в инспекторе (`RoomInspector.tsx`)**: строка каждой особи с типом цветом миниатюры и шкалой полученных ран.
- **Селектор `roomIntruders` (`utils/roomIntruders.ts`)**: карта и инспектор разрешают id отсека в сущности Чужих одним способом.
- **Тесты**: срез передаёт монстров исследованного отсека и их раны; селектор, значки карты и блок инспектора покрыты клиентскими тестами.

### Исправлено

- Пункт плана про Личинку приведён к книге правил: Личинка ставится миниатюрой в отсек, а заражает через свою Внезапную атаку без карты — код Шага 2 уже следует книге, изменений не потребовалось.

## 0.2.13 — 2026-09-21

Шаг 2 этапа 0.4.0: Контакт и Внезапная атака разыгрываются движком, в интерфейсе — модалка «КОНТАКТ!»:

### Добавлено

- **Контакт (`packages/shared/src/logic/contact.ts`, `fsm.ts`)**:
  - Второй маркер Шума в Коридоре или вентиляции ставит в очередь `CONTACT_INTERRUPT` вместо ошибки `CONTACT_NOT_IMPLEMENTED`.
  - Сброс всех маркеров Шума из Коридоров отсека и вентиляции, вытягивание жетона из мешка.
  - Пустой жетон возвращается в мешок (ретасование потоком `bag`) и шумит во все Коридоры отсека; последний жетон в мешке добавляет Взрослую Особь из запаса.
  - Монстр появляется в отсеке с отложенным жетоном (`IntruderEntity.token`); Внезапная атака — при числе карт на руке строго меньше числа Бегства.
  - Первый Контакт партии помечается флагом `isFirstContact` (заготовка сброса Целей).
- **Внезапная атака (`SURPRISE_ATTACK_INTERRUPT`)**:
  - Разбор всех 8 эффектов колоды Атак: Царапина, Укус, Атака когтями, Атака хвостом, Трансформация (с цепочкой при пустой руке), Ярость, Слизь, Зов; промах без символа атакующего.
  - Личинка заражает без карты: уходит с поля на планшет (`PlayerState.hasLarva`), +1 Заражение.
  - Раны и смерть: третья Лёгкая Травма превращается в Тяжёлую, четвёртая Травма при трёх Тяжёлых убивает; в отсеке остаются Труп и сброшенные Тяжёлые Объекты.
  - Гибель активного игрока завершает его ход; гибель всех персонажей завершает партию (`ALL_PLAYERS_DEAD`).
  - Пустые колоды Атак, Заражения и Травм тасуются из сброса потоком `combat`; двойное дно — явные ошибки.
- **Журнал**: события `CONTACT_OCCURRED`, `SURPRISE_ATTACK_TRIGGERED`, `SURPRISE_ATTACK_RESOLVED`, `INTRUDER_TRANSFORMED`, `INTRUDER_CALLED`, `PLAYER_DIED`, причина Шума `BLANK`; версия схемы состояния — **5** (старые сохранения не восстанавливаются).
- **Интерфейс**:
  - Модалка «КОНТАКТ!» (`ContactModal.tsx`): жетон, число Бегства, баннер первого Контакта, баннеры исхода Внезапной атаки; закрытие запоминается в сторе.
  - Русские сообщения и подсветка новых событий в журнале.

### Изменено

- Завершение партии в журнале различает взрыв корабля, разрыв обшивки и гибель всех персонажей.

## 0.2.12 — 2026-09-21

Шаг 1 этапа 0.4.0: контракт данных Чужих, колода Атак Чужих и кубик Боя. Только ядро правил, изменений интерфейса нет:

### Добавлено

- **Колода Атак Чужих (`packages/shared/src/data/intruderAttacks.ts`)**:
  - Тип карты расширен: стойкость (`toughness`), стрелка Отступления (`hasRetreat`), символы атакующих типов (`attackerTypes`).
  - 20 карт: Царапина ×4, Укус ×4, Атака когтями ×4, Атака хвостом ×2, Трансформация ×2, Ярость ×2, Слизь ×1, Зов ×1.
  - Колода тасуется при подготовке партии через поток `cards`; порядок воспроизводится по сиду.
- **Кубик Боя (`packages/shared/src/data/combatDie.ts`)**:
  - Грани `CombatDieFace`: Промах ×2, Хвост, Силуэты, 1 Рана, 2 Раны.
  - Бросок `rollCombatDie` через детерминированный RNG-поток `combat`.
- **Модель особи Чужого**: существующий `IntruderEntity` (`id`, `type`, `roomId`, `woundsCount`) покрывает контракт шага, изменений не потребовалось.
- **Пакет источника и golden-тесты**: таблицы `combat-die` и `intruder-attacks` в `doc/sources/data-sources.json` со статусом `EXTERNAL_UNVERIFIED` (состав граней и карт читается только с физических компонентов, сверки не было); тесты сверяют код с пакетом.

## 0.2.11 — 2026-09-18

Окно подробной информации о карте, встроенный интерфейс подтверждения действий карт без системных алертов, полная интеграция и использование предметов, обязательный выбор роли при старте новой игры:

### Добавлено

- **Модальное окно подробной информации о карте (`CardDetailsModal.tsx`)**:
  - Кнопка «Инфо» (иконка `Info`) на каждой карте в руке, в инвентаре и в слотах рук.
  - Подробный просмотр названия, типа карты, стоимости розыгрыша/действия, свойств (оружие, боезапас, компоненты крафта, одноразовость, тяжесть) и полного не обрезанного текста описания.
  - Быстрая кнопка «Использовать» прямо из модального окна подробностей.
- **Интерактивный розыгрыш карт действий без системного alert**:
  - При нажатии «Применить» на выбранной карте открывается внутриигровой интерфейс подтверждения с подробным описанием эффекта, требуемой стоимости и кнопками «Подтвердить» / «Отмена».
  - Добавлено действие `ACTION_PLAY_CARD` в ядро правил (`fsm.ts`) с оплатой стоимости и применением эффектов карт (перезарядка оружия, отдых и сканирование инфекции, ремонт отсека, снос дверей и т.д.).
- **Полная реализация и использование карт предметов (`ACTION_USE_ITEM`)**:
  - В инвентаре и слотах рук добавлены кнопки «Исп. [N]» и «Подробнее» для каждого найденного или экипированного предмета.
  - Движок правил исполняет `ACTION_USE_ITEM` с оплатой `actionCost` и применением эффектов предметов (лечение легких/тяжелых ран бинтами и аптечками, снятие слизи одеждой, тушение пожара огнетушителем, ремонт инструментов/изоленты, пополнение энергооружия энергозарядом, добор карт пайками и т.д.). Одноразовые предметы корректно списываются из инвентаря или рук.
- **Выбор персонажа при каждом старте новой игры**:
  - Кнопка перезапуска «Новая игра» в шапке HUD теперь всегда открывает модальное окно выбора персонажа, позволяя выбрать любой класс перед началом каждой новой экспедиции.

## 0.2.10 — 2026-09-18

Улучшения интерфейса карт, выбор персонажа перед стартом, отображение здоровья в шапке инвентаря и выбор номера коридора для шума при осторожном движении:

### Добавлено

- **Интерфейс карт руки (`PlayerHandPanel.tsx`)**:
  - Увеличена высота и внутренние отступы контейнера карт руки (`min-h-[160px]`, `w-36 h-32`).
  - Кнопка «Применить» перенесена вниз под карту в освободившееся место после сдвига вверх при выборе.
- **Выбор персонажа перед началом игры (`CharacterSelectModal.tsx`, `App.tsx`, `setup.ts`)**:
  - Модальное окно выбора персонажа при первом запуске или по нажатию кнопки «Персонаж» в HUD.
  - Поддержка выбора любого из 6 доступных классов (Капитан, Пилот, Учёный, Скаут, Солдат, Механик).
- **Символическое отображение здоровья в шапке руки**:
  - В строку руки рядом с инвентарем добавлено отображение статуса ранений (Лёгкие раны, Тяжёлые травмы) с привязкой к правому краю.
- **Осторожное движение по номерам коридоров (`RoomInspector.tsx`, `fsm.ts`)**:
  - При осторожном движении отображаются только уникальные номера коридоров (1..4) целевого отсека и технический коридор.
  - Если несколько коридоров имеют одинаковый номер, маркер шума добавляется во все коридоры с этим номером.

## 0.2.9 — 2026-09-18

Улучшения интерфейса руки игрока и базовых действий: пул очков действия, быстрое разыгрывание карты, подтверждение паса, поддержка осторожного движения в инспекторе отсеков и корректный пропуск незавершённой Фазы Событий.

### Добавлено

- **Пул и конвертация очков действия (`PlayerHandPanel.tsx`, `gameStore.ts`)**:
  - Кнопка «В очки действия» для конвертации выделенных карт руки в Action Points (AP).
  - Карты в пуле блокируются от розыгрыша и помечаются бейджем «В резерве».
  - Возможность частичной или полной отмены конвертации с возвратом карт в руку.
  - Автоматическое использование конвертированных очков действия и выделенных карт при оплате перемещения, осторожного движения, поиска и действий комнат.
- **Быстрое разыгрывание одиночной карты**:
  - При выборе ровно одной карты на руке над ней отображается кнопка «Применить» для удобного использования действия карты.
- **Подтверждение при пасе**:
  - Модальное окно подтверждения при нажатии кнопки «Пас», если у игрока ещё остались карты в руке или неизрасходованные очки действия.
- **Осторожное движение в `RoomInspector`**:
  - Возможность выбора «Осторожное движение [цена: 2]» при переходе между отсеками.
  - Интерактивный выбор целевого свободного коридора или вентиляции для размещения маркера шума.
- **Пропуск Фазы Событий (`turnCycle.ts`)**:
  - Запись события `EVENT_PHASE_SKIPPED` в игровой лог с информацией о разработке фазы и автоматический старт нового раунда, устраняющий блокировку игры при всеобщем пасе.

## 0.2.8 — 2026-09-18

Ревизия выполнения Шага 8 в рамках реализации версии 0.3.0: финальная интеграция, комплексное тестирование, валидация отсутствия утечек информации, проверка сериализации и обновление документации.

### Добавлено

- **Сквозные интеграционные тесты (`packages/shared/src/logic/release_v0_3_0.test.ts`)**:
  - Полный round-trip тест JSON-сериализации и десериализации состояния (проверка сохранения колод, руки, личного сброса и отложенного решения `pendingDecision`).
  - Тест строгого фильтра скрытой информации (`filterStateForPlayer`): гарантировано, что приватные решения (`CHOOSE_SEARCH_ITEM`) и вытянутые скрытые предметы цензурируются в `null` для любых других игроков, исключая читерство через клиентский код.
  - Тест атомарности: гарантирован откат игрового состояния движком при любых ошибках и нелегальных действиях игрока.
- **Обновление документации**:
  - Обновлён `doc/roadmap.md`: все 8 шагов этапа 3 (v0.3.0) отмечены как выполненные.
  - Обновлён `README.md`: отражены механики версии 0.3.0 (Фаза Игроков, карты Действий, Поиск, действия 11 базовых отсеков, интерфейс руки и модальных окон).

## 0.2.7 — 2026-09-18

Ревизия выполнения Шага 7 в рамках перехода к версии 0.3.0: пользовательский интерфейс Фазы Игроков, нижняя панель руки с читаемыми картами, выбор карт оплаты, скрытый инвентарь и слоты рук, модальные окна приватных решений (`DecisionModal`), контекстные кнопки действий и обыска в `RoomInspector`.

### Добавлено

- **Интерфейс руки игрока (`packages/client/src/components/hand/PlayerHandPanel.tsx`)**:
  - Карточная нижняя панель с отображением карт руки, их стоимости и описания.
  - Поддержка карт Заражения с индикацией статуса сканирования и инфекции.
  - Интерактивный мультиселект карт для оплаты действий и сброса.
  - Кнопка Паса (`ACTION_PASS`) с передачей выбранных карт для сброса.
  - Счётчик оставшихся действий в микроходе (0/2, 1/2).
  - Счётчики личной колоды и сброса.
  - Выдвижная панель инвентаря с отображением обоих слотов рук (оружие, боезапас, тяжёлые объекты), карманных предметов с цветовой кодировкой и счётчиков лёгких/тяжёлых травм.
- **Модальные окна приватных решений (`packages/client/src/components/modals/DecisionModal.tsx`)**:
  - `CHOOSE_WHITE_ROOM_DECK`: интерактивный выбор одной из трёх колод предметов (Красная, Жёлтая, Зелёная) при обыске белого отсека.
  - `CHOOSE_SEARCH_ITEM`: выбор 1 из 2 вытянутых карт предметов с возвратом второй карты под низ колоды.
  - `DISCARD_HEAVY_ITEM_FOR_NEW`: выбор тяжёлого предмета из рук для сброса при переполнении слотов рук.
- **Интеграция в инспектор отсека (`RoomInspector.tsx`)**:
  - Контекстная кнопка обыска отсека (`ACTION_SEARCH`) с проверкой условий и стоимости [цена: 1].
  - Контекстная кнопка использования консоли отсека (`ACTION_ROOM_ABILITY`) с проверкой неисправности и стоимости [цена: 2].
- **Компоновка и защита информации**:
  - Панели скомпонованы без наложения друг на друга: верхний HUD, карта, инспектор, рука, журнал и модальные диалоги.
  - Интерфейс работает строго через `SanitizedGameState`.
- **Тесты UI**:
  - Добавлены модульные тесты в `PlayerHandPanel.test.tsx` для проверки рендеринга руки и модальных окон решений.

## 0.2.6 — 2026-09-18

Ревизия выполнения Шага 6 в рамках перехода к версии 0.3.0: реализация базовых действий комнат категории «1» (`ACTION_ROOM_ABILITY`), списание стоимости в 2 карты действия, валидация неисправности и боя, изоляция эффектов для 11 комнат.

### Добавлено

- **Модуль действий комнат (`packages/shared/src/logic/roomAbilities.ts`)**:
  - `executeRoomAbility`: единая точка исполнения действий отсеков с общими проверками (исследованность отсека, отсутствие неисправности `hasMalfunction`, отсутствие Чужих/боя в отсеке, совпадение текущего отсека персонажа).
  - **Оружейная (`ARMORY`)**: пополнение боезапаса энергетического оружия в руках персонажа (+2 заряда до `maxAmmo`).
  - **Комната связи (`COMM_ROOM`)**: отправка сигнала бедствия (`hasSignalSent = true`).
  - **Медотсек (`INFIRMARY`)**: три режима лечения на выбор (перевязка всех тяжелых ран `TREAT_SERIOUS`, исцеление 1 перевязанной тяжелой раны `HEAL_SERIOUS`, исцеление всех легких ран `HEAL_LIGHT`).
  - **Генератор (`GENERATOR`)**: взведение и остановка таймера самоуничтожения с проверкой блокировки (запрет при наличии персонажей в Анабиозе, запрет отключения в необратимой зоне при `position >= 6`).
  - **Пожарная система (`FIRE_CONTROL`)**: тушение маркера пожара в выбранном целевом отсеке корабля.
  - **Склад (`STORAGE`)**: бесплатный поиск в выбранной колоде без уменьшения запаса предметов отсека (`itemsCount`).
  - **Улей (`NEST`)**: взятие яйца Чужих в свободный слот руки с уменьшением количества яиц на поле.
  - **Лаборатория (`LABORATORY`)**: анализ тяжелого объекта (Труп, Яйцо или Останки) из рук персонажа со сбросом объекта и раскрытием соответствующей карты Слабости Чужих.
  - **Кабины спасательных капсул (`ESCAPE_POD_A`, `ESCAPE_POD_B`)**: посадка в разблокированную капсулу со свободными местами.
  - **Хирургический отсек (`SURGERY`)**: сканирование всех карт Заражения на руке персонажа, удаление инфицированных карт, замешивание чистых в колоду, получение 1 легкой раны и завершение микрохода пасом.
- **Интеграция действия в движок `GameEngine`**:
  - `ACTION_ROOM_ABILITY`: проверка и списание стандартной стоимости в 2 карты действия с руки персонажа (`executeCardPayment`), выполнение эффекта отсека через `executeRoomAbility`.
- **Журнал событий**:
  - Добавлено событие `ROOM_ABILITY_USED` в общий лог ядра и форматирование сообщений в `gameLogModel.ts`.

## 0.2.5 — 2026-09-18

Ревизия выполнения Шага 5 в рамках перехода к версии 0.3.0: действие «Поиск», валидация условий отсека, выбор колоды в белых комнатах, интерактивный выбор 1 из 2 карт через `PendingDecision`, возврат невыбранной карты под низ колоды и уменьшение счётчика предметов отсека.

### Добавлено

- **Типы решений (`packages/shared/src/types/decisions.ts`)**:
  - `PendingDecision`: контракт отложенных решений игрока (`CHOOSE_SEARCH_ITEM`, `CHOOSE_WHITE_ROOM_DECK`, `DISCARD_HEAVY_ITEM_FOR_NEW`, и др.).
- **Модуль Поиска (`packages/shared/src/logic/search.ts`)**:
  - `validateSearchConditions`: строгая проверка возможности поиска (исследованность отсека, `itemsCount > 0`, отсутствие Чужих/боя, запрет поиска в Улье `NEST` и Комнате со Слизью `SLIME_ROOM`).
  - `getRoomDeckColor`: сопоставление отсека корабля с цветом колоды предметов (Красная, Жёлтая, Зелёная, Белая).
  - `drawSearchCards`: взятие до 2 карт из указанной колоды предметов.
  - `placeItemToPlayer`: размещение взятого предмета в инвентарь или в слоты рук (с инициированием решения замены тяжёлого предмета при занятых руках).
  - `finishSearch`: списание счётчика предметов комнаты (`itemsCount -= 1`), логирование и продвижение счётчика действий.
- **Интеграция действия «Поиск» в движок `GameEngine`**:
  - `ACTION_SEARCH`: списание оплаты (1 карта действия), проверка отсека, запрос выбора цвета в белых комнатах (`CHOOSE_WHITE_ROOM_DECK`), взятие 2 карт и переход в состояние `CHOOSE_SEARCH_ITEM`.
  - `ACTION_RESOLVE_DECISION`: выбор 1 карты, возврат второй карты под низ колоды добора, зачисление предмета игроку, уменьшение `itemsCount` в отсеке на 1 и передача хода при достижении 2 действий.
  - Обработка замены занятых слотов рук (`DISCARD_HEAVY_ITEM_FOR_NEW`) со сбросом старого предмета в сброс соответствующей колоды.
- **Защита информации (Sanitizer)**:
  - `filterStateForPlayer`: фильтрация `pendingDecision` — приватные решения выбора карт видны только их владельцу (`playerId`), для остальных игроков значение цензурируется в `null`.
- **Журнал событий**:
  - Событие `SEARCH_PERFORMED` регистрируется публично без раскрытия названия и характеристик найденного скрытого предмета.

## 0.2.4 — 2026-09-18

Ревизия выполнения Шага 4 в рамках перехода к версии 0.3.0: цикл микроходов Фазы Игроков, круговой порядок активного игрока, варианты микрораунда, блокировка спасовавших, урон от пожара при завершении хода и переход к Фазе Событий.

### Добавлено

- **Модуль цикла ходов и раундов (`packages/shared/src/logic/turnCycle.ts`)**:
  - `getOrderedPlayers`: упорядоченный список живых игроков по номеру хода (`orderNumber`).
  - `findNextActivePlayer`: поиск следующего неспасовавшего живого игрока по кругу.
  - `advanceTurn`: завершение микрохода с проверкой огня, обнулением счётчика выполненных действий микрохода и передачей хода следующему активному игроку (или переводом игры в `EVENT_PHASE` при общем пасе).
  - `applyFireEndTurnEffect`: начисление 1 Лёгкой Травмы за завершение микрохода в горящем отсеке (`FIRE_DAMAGE_TAKEN`).
  - `startNewRound`: запуск нового раунда с передачей жетона Первого Игрока, сбросом флагов паса, обнулением счётчиков действий и добором карт в руки до лимита.
- **Интеграция цикла микроходов в движок `GameEngine`**:
  - Проверка валидности фазы (`NOT_IN_PLAYER_PHASE`) и активного игрока (`NOT_ACTIVE_PLAYER`).
  - Блокировка действий спасовавших игроков (`PLAYER_ALREADY_PASSED`) до следующей Фазы Игроков.
  - Поддержка трёх легальных вариантов микрохода: два действия, одно действие и пас, немедленный пас.
  - Автоматическая передача хода после выполнения двух действий за микроход.
- **События журнала**:
  - Добавлены `ROUND_STARTED`, `PLAYER_TURN_STARTED`, `FIRE_DAMAGE_TAKEN`, расширен форматтер `gameLogModel.ts`.

## 0.2.3 — 2026-09-18

Ревизия выполнения Шага 3 в рамках перехода к версии 0.3.0: подготовка руки карт, лимиты, добор с перетасовкой сброса и универсальная система оплаты действий с запретом Заражения.

### Добавлено

- **Модуль подготовки руки и оплаты действий (`packages/shared/src/logic/cardsPayment.ts`)**:
  - `drawCardsToLimit`: добор карт в руку до целевого лимита персонажа; при пустой колоде добора личный сброс перетасовывается детерминированным RNG-потоком `cards`.
  - `getPlayerHandLimit`: расчёт предела руки по правилам игры (базовый 5 карт; 6 карт в исправных Каютах без Чужих при начале Фазы Игроков).
  - `validatePayment`: единая валидация оплаты действий сбросом карт Действий с руки (проверка точного количества, наличия в руке, отсутствия дубликатов, запрет оплаты картой за саму себя, абсолютный запрет использования карт Заражения).
  - `executeCardPayment`: атомарное списание карт оплаты из руки в личный сброс персонажа.
- **Интеграция оплаты в движок `GameEngine`**:
  - Действие «Движение» (`ACTION_MOVE`): требует и атомарно списывает 1 карту Действия с руки.
  - Действие «Осторожное движение» (`ACTION_CAREFUL_MOVE`): требует и атомарно списывает 2 карты Действий с руки.
  - Действие «Пас» (`ACTION_PASS`): объявление паса с возможностью сбросить любое количество карт с руки (как карт Действий, так и карт Заражения) и фиксацией в журнале партии (`PLAYER_PASSED`).
- **Инициализация руки персонажа**:
  - При создании партии персонаж сразу получает руку из 5 карт Действий и колоду добора из 5 оставшихся карт.

## 0.2.2 — 2026-09-18

Ревизия выполнения Шага 2 в рамках перехода к версии 0.3.0: наполнение колод Действий и Предметов, стартовое снаряжение, карты Заражения и Тяжёлых Травм.

### Добавлено

- **Личные колоды карт Действий (Action Cards)**:
  - 60 карт действий (по 10 карт на каждого из 6 классов: Капитан, Пилот, Механик, Солдат, Разведчик, Учёный) с именами, описаниями, ценами сброса (`playCost`) и классовыми ограничениями.
  - Детерминированная сборка и перетасовка личных колод персонажей через генератор `cards`.
- **Колоды Предметов (Item Cards) и Крафта**:
  - Наполнены 3 колоды предметов по 30 карт: Военные (Красные, 30 шт.), Технические (Жёлтые, 30 шт.), Медицинские (Зелёные, 30 шт.).
  - Синяя колода Создаваемых предметов: 12 карт (по 3 копии на каждый из 4 рецептов создания: Антидот, Тазер, Огнемёт, Коктейль Молотова).
  - Карты содержат компоненты крафта (`componentSymbols`), признак одноразовости, стоимость действий и флаги оружия/тяжёлых предметов.
- **Стартовое оружие персонажей**:
  - Стартовое вооружение для всех 6 классов со слотами рук, боезапасом и признаком энергооружия (Энерговинтовка Разведчика vs классическое оружие остальных).
- **Колоды Заражения и Тяжёлых Травм**:
  - 27 карт Заражения (7 с инфекцией, 20 стерильных).
  - 16 карт Тяжёлых Травм (по 4 каждого типа: Рука, Нога, Тело, Кровотечение).
- **Интеграция в подготовку партии (`createInitialGameState`)**:
  - Персонажи начинают с личной перетасованной колодой из 10 карт действий и экипированным стартовым оружием в руке.
  - Колоды стола инициализируются перетасованными колодами предметов, крафта, заражения и травм через поток `cards`.
- **Пакет источников данных и golden-тесты**:
  - Добавлена таблица `deck-composition` в `doc/sources/data-sources.json` с проверками в golden-тестах `sources.golden.test.ts`.

## 0.2.1 — 2026-09-18

Ревизия выполнения Шага 1 в рамках перехода к версии 0.3.0: фиксация контракта v0.3.0 и систематизация источников данных.

### Добавлено

- **Спецификация контракта v0.3.0** ([`doc/v0.3.0-contract.md`](doc/v0.3.0-contract.md)):
  - Зафиксированы границы и порядок Фазы Игроков: порядок игроков, два действия, одно действие и пас, немедленный пас, правила получения травмы от пожара при завершении хода и условия перехода в Фазу Событий.
  - Утвержден формат карт Действий (`ActionCard`), типизация их эффектов, ограничений применения и правила универсальной оплаты действий (запрет сброса Заражения).
  - Спроектирован интерфейс отложенных решений (`PendingDecision`), обеспечивающий детерминированное интерактивное взаимодействие при Поиске, выборе колод белых комнат, сбросе тяжёлых предметов и выборе параметров комнат.
  - Уточнена область действий комнат категории «1»: в v0.3.0 входят все 11 комнат (`BASIC_ROOMS_1`), определена безопасная изоляция зависимостей с этапами v0.4–v0.6 (Чужие, бой, визуальный красный сканер).
- Обновлена дорожная карта (`doc/roadmap.md`) с отметкой о выполнении Шага 1.

## 0.2.0 — 2026-09-18

Ревизия завершения этапа v0.2.0: перемещение, Туман войны, Шум и публичный журнал действий партии.

### Добавлено

- **Публичный журнал действий партии**: в общем ядре появился типизированный `GameLogEntry` с последовательными
  идентификаторами. Журнал начинается с `GAME_STARTED` и фиксирует перемещения с Коридором, обнаружение отсеков,
  жетоны Исследования и их эффекты, результаты и маркеры Шума, пропуски Шума, dev-изменения и окончание партии.
- **Нижняя панель журнала**: чёрная сворачиваемая панель подключена к отфильтрованному состоянию клиента. Русские
  сообщения форматируются сегментами с контрастными semantic tones и жирным выделением игроков, отсеков,
  Коридоров, эффектов и результатов. Кнопка показа/скрытия доступна через `aria-expanded`, `aria-controls` и
  текстовую подпись.
- Документация реализации вынесена в [`doc/game-log.md`](doc/game-log.md), README дополнен описанием журнала.

### Изменено

- **Контракт состояния — v4**: `gameLog` сохраняется вместе с партией. Сохранения старых версий или без журнала
  отклоняются, чтобы история не расходилась с игровым состоянием.
- Добавлены тесты последовательности событий, записи последствий перемещения, отката отклонённых действий,
  форматирования Шума и эффектов, а также защиты скрытых названий неисследованных отсеков.

## 0.1.11 — 2026-09-17

Ревизия «Этап 2 плана исправлений» ([doc/fix-plan-0.1.9.md](doc/fix-plan-0.1.9.md), Э2-1…Э2-6):
данные приведены к 1:1 с источником, движение и Шум доведены до конца, а недоделанное отклоняется
явной ошибкой.

### Добавлено

- **Пакет источника данных** ([`doc/sources/data-sources.json`](doc/sources/data-sources.json)):
  у каждой таблицы — происхождение каждого числа (строки `doc/rules.md`, подтверждение владельца
  компонентов, внешний источник) и список того, что ещё ждёт физической сверки. Golden-тесты
  `sources.golden.test.ts` сверяют код с пакетом: правка таблицы без правки источника роняет сборку (Э2-1).
- **Пул жетонов Исследования из коробки**: 20 жетонов и 44 предмета вместо 16 жетонов и 34 предметов.
  При подготовке тасуются все 20, по одному кладутся в 16 неособых отсеков, а 4 возвращаются
  в коробку — как в книге правил (стр. 6, шаг 4) (Э2-1).
- **Запас маркеров и правила Дверей** (`logic/markers.ts`): 30 маркеров Шума, 8 Пожара, 8 Неисправности,
  12 жетонов Дверей; лимит один маркер на Коридор и один на отсек; пустой запас Пожара взрывает
  корабль, пустой запас Неисправности разрывает обшивку, и партия заканчивается явной причиной;
  при пустом запасе Дверей жетон переставляется с поля, а Разрушенные Двери для перестановки
  не разбираются (стр. 17) (Э2-2).
- **«Осторожное движение»** (стр. 13): вместо броска кубика Шума маркер кладётся в выбранный игроком
  Коридор, ведущий в отсек, включая поле Технических Коридоров при наличии Входа; запрещено в Бою
  и когда все ведущие Коридоры уже помечены (Э2-3).
- **Завершение вскрытия и Шума**: эффекты жетона (Тишина, Опасность, Слизь, Пожар, Неисправность,
  Двери) разыгрываются целиком, «Двери» закрывают Коридор входа, Слизь превращает «Тишину»
  в «Опасность», маркеры ставятся и в Коридорах, и на поле Технических Коридоров (Э2-3).
- **Срезы колод** (Э2-5): наружу уходят только размеры стопок — число карт в колоде добора,
  на руке и в сбросе; карты чужой руки и чужого сброса не покидают движок; у колод Заражения,
  Слабостей и Целей скрыто и число карт сброса, а невскрытая Слабость остаётся рубашкой вверх.
  Число карт на руке видно всем: с ним сравнивается число на жетоне Чужого при Внезапной атаке (стр. 18).

### Изменено

- **Разрушенная Дверь стала терминальной**: переход `OPEN → CLOSED → DESTROYED` больше не циклится,
  и dev-переключатель не может «починить» разрушенную Дверь (стр. 17). Подпись в dev-панели
  показывает это же, а не обещает возврат (Э2-2).
- **Контракт состояния — v3** (`GAME_STATE_SCHEMA_VERSION = 3`): у Пула Чужих появился запас жетонов
  рядом с полем, у партии — причина окончания, у прерывания Шума — режим разыгрывания. Старое или
  битое сохранение по-прежнему даёт новую партию, но теперь отказ явный: запись без полей текущего
  контракта не доигрывается «на угад» (Э2-6).
- **Пул Чужих собирается из полного набора 27 жетонов**: мешок — это выборка из коробки по правилу
  подготовки, остальные жетоны лежат рядом с полем и войдут в игру вместе с Контактом (Э2-1).
- **Пакет источника и README**: утверждение «1:1» теперь опирается на ссылки на источник, а не на
  декларацию; непроверенные данные помечены и перечислены (Э2-1).

### Известные ограничения

- Числа на обороте жетонов Чужих (проверка Внезапной атаки) и топология Коридоров читаются только
  с физических компонентов: они помечены в пакете источника как `EXTERNAL_UNVERIFIED`
  и `UNVERIFIED_BOARD`. Пока сверки нет, парные Коридоры описаны одной связью, а бросок Шума
  на отсутствующий номер выхода разыгрывается как «Тишина».
- Контакт, вытягивание жетонов из мешка, бой и колоды — этапы 3–4 дорожной карты.

## 0.1.10 — 2026-09-17

Ревизия «Этап 1 плана исправлений» ([doc/fix-plan-0.1.9.md](doc/fix-plan-0.1.9.md)):
критерии 0.1.0 снова выполняются, полумеханики не остаётся — незавершённое отклоняется явной ошибкой.

### Исправлено

- **Мешок Чужих тасуется** при подготовке партии потоком случайности `bag`, а в срез игрока
  уходит не порядок жетонов, а состав по типам: Контакт больше не был бы предсказуем (Э1-1).
- **Сид партии не зависит от `crypto.randomUUID`**: в небезопасном контексте (`http://` с LAN-адреса,
  игра с телефона) UUIDv4 собирается из `crypto.getRandomValues`, а без Web Crypto рождается явная
  ошибка вместо белого экрана (Э1-2).
- **Вскрытие отсека разыгрывается целиком** (Э1-3): эффект жетона Исследования (Пожар, Неисправность,
  Слизь, Двери, Тишина, Опасность), затем бросок кубика Шума с гранями 1, 1, 2, 2, 3, 3, 4, 4,
  Тишина, Опасность — номер ставит маркер в Коридор (включая Технические Коридоры), «Опасность»
  помечает все пустые Коридоры отсека, «Тишина» отменяет бросок, Слизь превращает её в «Опасность».
  Если в отсеке есть другой персонаж, бросок не делается; Контакт (маркер в занятом Коридоре)
  отклоняется явной ошибкой до реализации Пула Чужих (этап 4).
- **Порядок добора личных колод не раскрывается** даже владельцу, чужая рука скрыта, сброс
  остаётся открытым — как в настольной игре (Э1-4).

### Добавлено

- **Замки на критерии 0.1.0 в тестах** (Э1-4): состав мешка вместо порядка, скрытые эффекты
  невскрытых отсеков, запрет отладочных действий без флага, отсутствие отладочной панели
  в dev-режиме и в продакшн-сборке, восстановление партии из сохранения.
- **CI-минимум** (Э1-5): GitHub Actions на push и pull request — `npm ci`, `npm run verify`,
  продакшн-сборка и проверка, что в бандле нет строк отладочной панели.
- **Счётчики потоков случайности в состоянии партии** (`meta.rngDraws`): броски Шума продолжаются
  после сохранения и перезагрузки, а не начинаются заново (нужно для Э1-3; полноценный блок — Э2-4).
- **Версия приложения в HUD** берётся из `package.json` через `define` сборки — расхождения
  «в README одна версия, в интерфейсе другая» больше не возможны (Э1-6).
- **CHANGELOG и раздел документации** в README: ссылки ведут на существующие документы
  (`doc/review-0.1.9.md`, `doc/fix-plan-0.1.9.md`), целевые GDD и техстек помечены как «цель».

### Изменено

- **Контракт состояния — версия 2**: у отсека появился эффект жетона Исследования
  (`explorationEffect`), прерывание вскрытия несёт Коридор входа, у мета-состояния появились
  счётчики случайности. Старые сохранения (v1) не восстанавливаются: партия начинается заново —
  это осознанное решение, пока игра не вышла за пределы dev-ревизий.

## 0.1.9 — 2026-09-17

Базовая ревизия, разобранная в [doc/review-0.1.9.md](doc/review-0.1.9.md): карта корабля и инспектор
отсека, подготовка партии по сиду, стек прерываний, фильтр скрытой информации, офлайн-сохранение.
Известные дефекты ревизии — в плане исправлений (Э1-1…Э1-6 закрыты в 0.1.10).
